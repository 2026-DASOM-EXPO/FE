REACT_APP_API_URL=http://localhost:8080/api

# 알림(Alert) SSE 실시간 스트림 사용 여부 (백엔드 GET /alerts/stream)
REACT_APP_ENABLE_ALERT_SSE=false

# 장비 착용상태 / 센서값 SSE 실시간 스트림 사용 여부 (백엔드 GET /realtime/stream)
# 백엔드에 equipment-status, sensor-update 이벤트가 준비되면 true로 전환
REACT_APP_ENABLE_DEVICE_SSE=false
